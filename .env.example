# Optional local scratch file — NOTHING reads this automatically.
# It's just a convenient place to keep the keys you paste into the Worker's
# `wrangler secret put` prompts (or the extension's Options page in direct
# mode). Copy to `.env` and fill in. The real `.env` is gitignored.

anthropic_key=
eleven_labs_api_key=
